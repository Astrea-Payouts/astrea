# astrea (Cargo workspace)

Workspace root for the Soroban contracts. There is one contract,
[`contracts/event-escrow`](contracts/event-escrow), and no `hello_world`
template left over.

How to build, test and deploy is in [`../README.md`](../README.md). Run every
`cargo` command from this directory.
